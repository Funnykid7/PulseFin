package com.pulsefin.core.designsystem.theme

import androidx.compose.material3.Typography

// M3 Expressive leans on large display typography for hero moments (e.g. "Your Mix",
// Now Playing title). Kept as the default M3 type scale for now; swap in a custom
// expressive scale + variable font when the design system work lands.
internal val PulseTypography = Typography()
